{
  "name": "fastify-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "set-webhook": "node --env-file=.env set-webhook.js",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}
